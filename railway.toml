[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn web.app:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 300 --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
